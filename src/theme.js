import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#0092AB',
      light: '#00AECC',
      dark: '#2D4260'
    },
    secondary: {
      main: '#edf2ff',
      dark: '#737373',
      light: '#EBECF0'
    },
  },
});